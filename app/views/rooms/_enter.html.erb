<%= link_to "放鳥する",
            pet_in_rooms_path(pet_id: pet.id, room_id: room.id),
            id: "enter-button-for-pet-#{pet.id}",
            class: "float-right",
            data: { turbo_method: :post } %>
