<div class="container">
  <div class="grid sm:grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-4">
    <% @birds.each do |bird| %>
      <%= render 'bird', { bird: bird } %>
    <% end %>
  </div>
</div>
